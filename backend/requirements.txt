# HT Activewear backend — minimal runtime dependencies
# Previous 73-package requirements.txt is preserved in requirements.txt.bloated-backup
# Slimmed on 2026-04-09 after OOM crash during Coolify build (numpy/pandas/cryptography
# Rust compile exhausted server RAM). Pip resolves transitive deps automatically.

# Web framework
fastapi==0.110.1
uvicorn==0.25.0
python-multipart==0.0.20

# Data validation (pydantic[email] pulls email-validator for EmailStr)
pydantic[email]==2.12.5

# HTTP & Airtable
httpx==0.28.1
pyairtable==3.3.0

# Auth & config
bcrypt==4.1.3
python-dotenv==1.2.1
